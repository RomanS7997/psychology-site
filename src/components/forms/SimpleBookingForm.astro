---
// src/components/forms/SimpleBookingForm.astro
// Упрощенная форма записи без выбора специалиста
import { url } from '../../utils/url';
---

<div class="booking-modal-overlay" id="booking-modal" role="dialog" aria-modal="true" aria-labelledby="booking-modal-title">
  <div class="booking-modal-content max-w-2xl">
    <button class="modal-close" onclick="closeBookingModal()" aria-label="Закрыть">
      <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
      </svg>
    </button>
    
    <div class="booking-content">
      <h2 id="booking-modal-title" class="text-3xl font-bold text-gray-900 mb-2">Записаться</h2>
      <p class="text-gray-600 mb-8">Заполните форму, и мы свяжемся с вами для подтверждения записи</p>

      <!-- Экран успеха (скрыт по умолчанию) -->
      <div id="booking-success" class="booking-success hidden" aria-hidden="true">
        <div class="success-confetti" aria-hidden="true">
          <span></span><span></span><span></span><span></span><span></span>
          <span></span><span></span><span></span><span></span><span></span>
          <span></span><span></span><span></span><span></span><span></span>
        </div>
        <div class="success-icon">
          <svg viewBox="0 0 80 80" width="80" height="80" aria-hidden="true">
            <circle class="success-ring" cx="40" cy="40" r="36" fill="none" stroke="currentColor" stroke-width="4"/>
            <path class="success-check" d="M24 42 L36 54 L58 30" fill="none" stroke="currentColor" stroke-width="5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </div>
        <h3 class="success-title">Заявка отправлена!</h3>
        <p class="success-text">Спасибо! Мы свяжемся с вами в ближайшее время для подтверждения записи.</p>
        <button type="button" onclick="closeBookingModal()" class="success-btn">
          Хорошо
        </button>
      </div>

      <!-- Инлайн-ошибка (скрыта по умолчанию) -->
      <div id="booking-error" class="booking-error hidden" role="alert" aria-hidden="true">
        <span class="error-icon" aria-hidden="true">⚠️</span>
        <span>Не удалось отправить заявку. Позвоните нам: <a href="tel:+79151955132" class="underline font-semibold">+7 (915) 195-51-32</a></span>
      </div>

      <form id="booking-form" class="space-y-6">

        <!-- Имя -->
        <div>
          <label for="name" class="block text-sm font-semibold text-gray-900 mb-2">
            Ваше имя <span style="color: var(--spark-deep)">*</span>
          </label>
          <input 
            type="text" 
            id="name" 
            name="name" 
            required
            class="form-field"
            placeholder="Введите ваше имя"
          >
        </div>

        <!-- Телефон -->
        <div>
          <label for="phone" class="block text-sm font-semibold text-gray-900 mb-2">
            Телефон <span style="color: var(--spark-deep)">*</span>
          </label>
          <input 
            type="tel" 
            id="phone" 
            name="phone" 
            required
            class="form-field"
            placeholder="+7 (___) ___-__-__"
          >
        </div>

        <!-- Email -->
        <div>
          <label for="email" class="block text-sm font-semibold text-gray-900 mb-2">
            Email
          </label>
          <input 
            type="email" 
            id="email" 
            name="email"
            class="form-field"
            placeholder="your@email.com"
          >
        </div>

        <!-- Комментарий -->
        <div>
          <label for="comment" class="block text-sm font-semibold text-gray-900 mb-2">
            Комментарий
          </label>
          <textarea 
            id="comment" 
            name="comment"
            rows="3"
            class="form-field resize-none"
            placeholder="Расскажите кратко о вашей ситуации (необязательно)"
          ></textarea>
        </div>

        <!-- Согласие на обработку данных -->
        <div class="bg-gray-50 p-4 rounded-xl">
          <label class="flex items-start gap-3 cursor-pointer">
            <input 
              type="checkbox" 
              name="privacy" 
              required
              class="mt-1 w-5 h-5 rounded" style="accent-color: var(--spark-deep)"
            >
            <span class="text-sm text-gray-700">
              Я согласен(а) на обработку персональных данных в соответствии с 
              <a href={url('/privacy')} target="_blank" style="color: var(--spark-deep)" class="underline">
                Политикой конфиденциальности
              </a>
            </span>
          </label>
        </div>

        <!-- Кнопка отправки -->
        <div class="flex gap-4">
          <button 
            type="submit"
            class="pill pill-solid flex-1 justify-center"
          >
            Отправить заявку
          </button>
          <button 
            type="button"
            onclick="closeBookingModal()"
            class="pill pill-outline"
          >
            Отмена
          </button>
        </div>

        <!-- Инфо -->
        <p class="text-sm text-gray-500 text-center">
          После отправки заявки мы свяжемся с вами в ближайшее время для подтверждения записи
        </p>
      </form>
    </div>
  </div>
</div>

<style>
  .form-field {
    width: 100%;
    padding: 12px 16px;
    border: 1px solid var(--stone);
    border-radius: 10px;
    background: var(--canvas);
    color: var(--ink);
    font-size: 15px;
    transition: border-color .2s ease;
  }
  .form-field:focus-visible {
    outline: 2px solid var(--spark-deep);
    outline-offset: 2px;
    border-color: var(--spark-water);
  }

  .booking-modal-overlay {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.7);
    backdrop-filter: blur(4px);
    z-index: 9999;
    overflow-y: auto;
    padding: 2rem 1rem;
  }

  .booking-modal-overlay.active {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .booking-modal-content {
    background: white;
    border-radius: 24px;
    width: 100%;
    max-width: 600px;
    position: relative;
    padding: 2.5rem;
    margin: auto;
    box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
    animation: modalSlideIn 0.3s ease-out;
  }

  @keyframes modalSlideIn {
    from {
      opacity: 0;
      transform: translateY(-30px) scale(0.95);
    }
    to {
      opacity: 1;
      transform: translateY(0) scale(1);
    }
  }

  .modal-close {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: #f3f4f6;
    color: #6b7280;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    border: none;
    transition: all 0.2s;
    z-index: 10;
  }

  .modal-close:hover {
    background: #e5e7eb;
    color: #374151;
    transform: rotate(90deg);
  }

  @media (max-width: 640px) {
    .booking-modal-content {
      padding: 1.5rem;
      border-radius: 16px;
    }
  }

  /* ---------- Экран успеха ---------- */
  .booking-success {
    position: relative;
    text-align: center;
    padding: 1.5rem 0.5rem 0.5rem;
    animation: successFadeIn 0.5s ease-out both;
  }

  .booking-success.hidden { display: none; }

  @keyframes successFadeIn {
    from { opacity: 0; transform: translateY(12px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  .success-icon {
    display: inline-flex;
    color: var(--spark-deep);
    margin-bottom: 1.25rem;
    filter: drop-shadow(0 8px 20px rgba(72, 148, 178, 0.35));
  }

  .success-icon svg { overflow: visible; }

  .success-ring {
    stroke-dasharray: 226;
    stroke-dashoffset: 226;
    animation: drawRing 0.55s 0.05s ease-out forwards;
    transform-origin: 40px 40px;
  }

  .success-check {
    stroke-dasharray: 60;
    stroke-dashoffset: 60;
    animation: drawCheck 0.35s 0.55s ease-out forwards;
  }

  @keyframes drawRing {
    to { stroke-dashoffset: 0; }
  }

  @keyframes drawCheck {
    to { stroke-dashoffset: 0; }
  }

  .success-title {
    font-size: 1.75rem;
    font-weight: 700;
    color: #0f172a;
    margin-bottom: 0.75rem;
    animation: successPop 0.4s 0.65s ease-out both;
  }

  .success-text {
    color: #475569;
    line-height: 1.6;
    margin-bottom: 1.75rem;
    animation: successPop 0.4s 0.8s ease-out both;
  }

  @keyframes successPop {
    from { opacity: 0; transform: scale(0.92); }
    to   { opacity: 1; transform: scale(1); }
  }

  .success-btn {
    padding: 0.875rem 2.5rem;
    background: linear-gradient(135deg, var(--spark-deep) 0%, var(--spark-lilac) 100%);
    color: white;
    border: none;
    border-radius: 0.75rem;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    box-shadow: 0 6px 20px rgba(72, 148, 178, 0.35);
    animation: successPop 0.4s 0.95s ease-out both;
  }

  .success-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 10px 24px rgba(72, 148, 178, 0.45);
  }

  /* Конфетти */
  .success-confetti {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: visible;
  }

  .success-confetti span {
    position: absolute;
    left: 50%;
    top: 60px;
    width: 10px;
    height: 10px;
    border-radius: 2px;
    opacity: 0;
    animation: confettiBurst 1.1s 0.6s ease-out forwards;
  }

  .success-confetti span:nth-child(1)  { background: var(--spark-water); --tx:  -160px; --ty: -120px; --r: -40deg; }
  .success-confetti span:nth-child(2)  { background: var(--spark-powder); --tx:   140px; --ty: -140px; --r:  50deg; }
  .success-confetti span:nth-child(3)  { background: #f2b134; --tx:  -220px; --ty:  -40px; --r: -80deg; }
  .success-confetti span:nth-child(4)  { background: #7fb685; --tx:   220px; --ty:  -20px; --r:  90deg; }
  .success-confetti span:nth-child(5)  { background: var(--spark-water); --tx:   -80px; --ty: -180px; --r: -20deg; border-radius: 50%; }
  .success-confetti span:nth-child(6)  { background: var(--spark-powder); --tx:    80px; --ty: -180px; --r:  20deg; border-radius: 50%; }
  .success-confetti span:nth-child(7)  { background: var(--spark-lilac); --tx:  -260px; --ty:   40px; --r: -110deg; }
  .success-confetti span:nth-child(8)  { background: #f2b134; --tx:   260px; --ty:   60px; --r:  110deg; }
  .success-confetti span:nth-child(9)  { background: #7fb685; --tx:  -120px; --ty:  -80px; --r: -60deg; border-radius: 50%; }
  .success-confetti span:nth-child(10) { background: var(--spark-water); --tx:   120px; --ty:  -90px; --r:  60deg; border-radius: 50%; }
  .success-confetti span:nth-child(11) { background: var(--spark-powder); --tx:  -200px; --ty:  120px; --r: -140deg; }
  .success-confetti span:nth-child(12) { background: var(--spark-lilac); --tx:   200px; --ty:  140px; --r:  140deg; }
  .success-confetti span:nth-child(13) { background: #f2b134; --tx:   -40px; --ty: -220px; --r:   10deg; border-radius: 50%; }
  .success-confetti span:nth-child(14) { background: #7fb685; --tx:    40px; --ty: -220px; --r:  -10deg; border-radius: 50%; }
  .success-confetti span:nth-child(15) { background: var(--spark-water); --tx:     0px; --ty: -260px; --r:   30deg; }

  @keyframes confettiBurst {
    0% {
      opacity: 0;
      transform: translate(-50%, 0) rotate(0deg) scale(0.6);
    }
    15% {
      opacity: 1;
    }
    100% {
      opacity: 0;
      transform: translate(calc(-50% + var(--tx)), var(--ty)) rotate(var(--r)) scale(1);
    }
  }

  /* ---------- Ошибка ---------- */
  .booking-error {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    background: #fef2f2;
    border: 1px solid #fecaca;
    color: #991b1b;
    padding: 1rem 1.25rem;
    border-radius: 0.75rem;
    margin-bottom: 1rem;
    animation: errorShake 0.5s ease-out;
  }

  .booking-error.hidden { display: none; }

  .booking-error .error-icon { font-size: 1.25rem; flex-shrink: 0; }

  @keyframes errorShake {
    0%, 100% { transform: translateX(0); }
    20%      { transform: translateX(-6px); }
    40%      { transform: translateX(6px); }
    60%      { transform: translateX(-4px); }
    80%      { transform: translateX(4px); }
  }

  @media (prefers-reduced-motion: reduce) {
    .booking-success,
    .success-ring,
    .success-check,
    .success-title,
    .success-text,
    .success-btn,
    .success-confetti span,
    .booking-error {
      animation: none !important;
    }
    .success-ring, .success-check { stroke-dashoffset: 0; }
  }
</style>

<script>
  function resetBookingView() {
    const form = document.getElementById('booking-form');
    const success = document.getElementById('booking-success');
    const errorBox = document.getElementById('booking-error');
    if (form) form.classList.remove('hidden');
    if (success) {
      success.classList.add('hidden');
      success.setAttribute('aria-hidden', 'true');
      // Перезапуск CSS-анимаций: форсируем reflow, сбрасывая классы
      success.style.animation = 'none';
      void success.offsetWidth;
      success.style.animation = '';
    }
    if (errorBox) {
      errorBox.classList.add('hidden');
      errorBox.setAttribute('aria-hidden', 'true');
    }
  }

  // фокус: запоминаем, куда вернуть, и замыкаем перебор внутри окна
  let bookingReturnFocus = null;

  function focusablesIn(root) {
    return Array.prototype.filter.call(
      root.querySelectorAll('a[href], button:not([disabled]), input, textarea, select, [tabindex]:not([tabindex="-1"])'),
      function (el) { return el.offsetParent !== null; }
    );
  }

  function trapBookingFocus(e) {
    const modal = document.getElementById('booking-modal');
    if (!modal || !modal.classList.contains('active') || e.key !== 'Tab') return;
    const items = focusablesIn(modal);
    if (!items.length) return;
    const first = items[0];
    const last = items[items.length - 1];
    if (e.shiftKey && document.activeElement === first) {
      e.preventDefault();
      last.focus();
    } else if (!e.shiftKey && document.activeElement === last) {
      e.preventDefault();
      first.focus();
    }
  }

  document.addEventListener('keydown', trapBookingFocus);

  function openBookingModal() {
    bookingReturnFocus = document.activeElement;
    const modal = document.getElementById('booking-modal');
    if (modal) {
      resetBookingView();
      modal.classList.add('active');
      document.body.style.overflow = 'hidden';
      // переводим фокус внутрь окна, иначе он остаётся на странице позади
      const first = focusablesIn(modal)[0];
      if (first) setTimeout(function () { first.focus(); }, 40);
    }
  }

  function closeBookingModal() {
    const modal = document.getElementById('booking-modal');
    if (modal) {
      modal.classList.remove('active');
      document.body.style.overflow = '';
      // возвращаем фокус туда, откуда окно открыли
      if (bookingReturnFocus && typeof bookingReturnFocus.focus === 'function') {
        bookingReturnFocus.focus();
        bookingReturnFocus = null;
      }
    }
  }

  // Закрытие по клику вне модального окна
  document.getElementById('booking-modal')?.addEventListener('click', (e) => {
    if (e.target.id === 'booking-modal') {
      closeBookingModal();
    }
  });

  // Закрытие по Escape
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') {
      closeBookingModal();
    }
  });

  // Обработка формы — отправка через FormSubmit.co на lyapina_sorokina@mail.ru
  document.getElementById('booking-form')?.addEventListener('submit', async (e) => {
    e.preventDefault();

    const form = e.target;
    const formData = new FormData(form);

    const payload = {
      _subject: 'Новая заявка с сайта — запись на консультацию',
      _template: 'table',
      _captcha: 'false',
      Имя: formData.get('name') || '—',
      Телефон: formData.get('phone') || '—',
      Email: formData.get('email') || '—',
      Комментарий: formData.get('comment') || '—',
      Источник: 'Форма записи (julialyapina.ru)'
    };

    const submitBtn = form.querySelector('button[type="submit"]');
    const originalText = submitBtn ? submitBtn.textContent : '';
    const errorBox = document.getElementById('booking-error');
    const successBox = document.getElementById('booking-success');

    if (errorBox) {
      errorBox.classList.add('hidden');
      errorBox.setAttribute('aria-hidden', 'true');
    }
    if (submitBtn) {
      submitBtn.disabled = true;
      submitBtn.textContent = 'Отправка...';
    }

    try {
      const response = await fetch('https://formsubmit.co/ajax/lyapina_sorokina@mail.ru', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Accept': 'application/json'
        },
        body: JSON.stringify(payload)
      });

      if (!response.ok) throw new Error('Ошибка отправки');

      // Показываем красивый экран успеха
      form.reset();
      form.classList.add('hidden');
      if (successBox) {
        successBox.classList.remove('hidden');
        successBox.setAttribute('aria-hidden', 'false');
        // Форсируем перезапуск анимаций
        successBox.style.animation = 'none';
        void successBox.offsetWidth;
        successBox.style.animation = '';
      }
    } catch (err) {
      if (errorBox) {
        errorBox.classList.remove('hidden');
        errorBox.setAttribute('aria-hidden', 'false');
        // Перезапуск shake-анимации
        errorBox.style.animation = 'none';
        void errorBox.offsetWidth;
        errorBox.style.animation = '';
      }
    } finally {
      if (submitBtn) {
        submitBtn.disabled = false;
        submitBtn.textContent = originalText;
      }
    }
  });

  // Форматирование телефона (фикс: разделители не дописываются
  // пока следующего разряда нет — backspace удаляет по одной цифре)
  const phoneInput = document.getElementById('phone');
  phoneInput?.addEventListener('input', (e) => {
    let digits = e.target.value.replace(/\D/g, '');
    if (digits.startsWith('7') || digits.startsWith('8')) {
      digits = digits.substring(1);
    }
    digits = digits.substring(0, 10);

    if (digits.length === 0) {
      e.target.value = '';
      return;
    }

    let formatted = '+7 (' + digits.substring(0, 3);
    if (digits.length > 3) formatted += ') ' + digits.substring(3, 6);
    if (digits.length > 6) formatted += '-' + digits.substring(6, 8);
    if (digits.length > 8) formatted += '-' + digits.substring(8, 10);

    e.target.value = formatted;
  });

  // Глобальные функции для доступа из других скриптов
  window.openBookingModal = openBookingModal;
  window.closeBookingModal = closeBookingModal;
</script>
